# Copy to .env.local for local overrides (never commit .env.local)

# CloudFront URL for static assets (leave empty for local dev)
NEXT_PUBLIC_ASSET_PREFIX=

# AWS region (used by the Lambda handler for display purposes)
AWS_REGION=eu-west-1
